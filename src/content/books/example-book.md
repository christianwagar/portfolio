---
---

Replace this with your review. The homepage shelf stays a one-sentence note; this page is the longer take — why the book mattered, what you kept, what you would skip.

When you swap in a real title, rename this file to match the `slug` in `src/data/site.ts` (for example `atomic-habits.md`) and update the Amazon `link` there if you have one.
